---
"@kapao-kit/ui": patch
---

Publish `globals.css` with Tailwind v4 `@source` for compiled `dist/*.js`, copy it in the `tsup` build, and document the import-based setup for consuming applications.
